/**
 * The type of the SLO
 */
export enum SLOType {
  METRIC = 'metric',
  MONITOR = 'monitor',
}

/**
 * The type of the Timeframe
 */
export enum TimeframeType {
  SEVENDAYS = '7d',
  THIRTYDAYS = '30d',
  NINETYDAYS = '90d',
}
